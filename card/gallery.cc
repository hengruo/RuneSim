//
// Created by Hengruo Zhang on 3/29/20.
//
#include "gallery.h"

void init_gallery_() {
  Card *card = nullptr;
  card = new DisciplineOfFortitude1;
  gallery[card->id] = card;
  card = new Draven1;
  gallery[card->id] = card;
  card = new Dawnspeakers;
  gallery[card->id] = card;
  card = new YusariTheSlayer1;
  gallery[card->id] = card;
  card = new Jinx_sGetExcited_1;
  gallery[card->id] = card;
  card = new GlacialStorm1;
  gallery[card->id] = card;
  card = new Teemo1;
  gallery[card->id] = card;
  card = new Lucian;
  gallery[card->id] = card;
  card = new Yasuo1;
  gallery[card->id] = card;
  card = new Heimerdinger;
  gallery[card->id] = card;
  card = new KeeperOfMasks;
  gallery[card->id] = card;
  card = new Tryndamere_sBattleFury1;
  gallery[card->id] = card;
  card = new AugmentedExperimenter;
  gallery[card->id] = card;
  card = new Mobilize;
  gallery[card->id] = card;
  card = new CullingStrike;
  gallery[card->id] = card;
  card = new ParadeElectrorig;
  gallery[card->id] = card;
  card = new Heimerdinger1;
  gallery[card->id] = card;
  card = new PhantomPrankster;
  gallery[card->id] = card;
  card = new Lux_sPrismaticBarrier1;
  gallery[card->id] = card;
  card = new AcademyProdigy;
  gallery[card->id] = card;
  card = new HeraldOfSpring;
  gallery[card->id] = card;
  card = new FaeBladetwirler;
  gallery[card->id] = card;
  card = new CrimsonCurator;
  gallery[card->id] = card;
  card = new IcebornLegacy;
  gallery[card->id] = card;
  card = new Ghost;
  gallery[card->id] = card;
  card = new OmenHawk;
  gallery[card->id] = card;
  card = new HarshWinds;
  gallery[card->id] = card;
  card = new GlimpseBeyond;
  gallery[card->id] = card;
  card = new Elise_sCrawlingSensation1;
  gallery[card->id] = card;
  card = new Braum;
  gallery[card->id] = card;
  card = new AncientCrocolith;
  gallery[card->id] = card;
  card = new SwiftwingLancer;
  gallery[card->id] = card;
  card = new SoulShepherd;
  gallery[card->id] = card;
  card = new EagerApprentice;
  gallery[card->id] = card;
  card = new Darius;
  gallery[card->id] = card;
  card = new KinkouLifeblade;
  gallery[card->id] = card;
  card = new RimefangWolf;
  gallery[card->id] = card;
  card = new VanguardDefender;
  gallery[card->id] = card;
  card = new Battlesmith;
  gallery[card->id] = card;
  card = new TiannaCrownguard;
  gallery[card->id] = card;
  card = new ArachnoidHost;
  gallery[card->id] = card;
  card = new Karma1;
  gallery[card->id] = card;
  card = new Garen1;
  gallery[card->id] = card;
  card = new StandAlone;
  gallery[card->id] = card;
  card = new Jinx;
  gallery[card->id] = card;
  card = new DecisiveManeuver;
  gallery[card->id] = card;
  card = new Fiora;
  gallery[card->id] = card;
  card = new PuffcapPeddler;
  gallery[card->id] = card;
  card = new BrightsteelFormation;
  gallery[card->id] = card;
  card = new SuperMegaDeathRocket_1;
  gallery[card->id] = card;
  card = new Elise;
  gallery[card->id] = card;
  card = new RelentlessPursuit;
  gallery[card->id] = card;
  card = new MagnumOpus1;
  gallery[card->id] = card;
  card = new ParalyzingBite1;
  gallery[card->id] = card;
  card = new Transfusion;
  gallery[card->id] = card;
  card = new OnslaughtOfShadows;
  gallery[card->id] = card;
  card = new LegionSaboteur;
  gallery[card->id] = card;
  card = new Hecarim1;
  gallery[card->id] = card;
  card = new ScrapScuttler1;
  gallery[card->id] = card;
  card = new Zed1;
  gallery[card->id] = card;
  card = new Vilemaw1;
  gallery[card->id] = card;
  card = new GreengladeElder;
  gallery[card->id] = card;
  card = new Darius1;
  gallery[card->id] = card;
  card = new Lucian_sRelentlessPursuit1;
  gallery[card->id] = card;
  card = new Jury_rig;
  gallery[card->id] = card;
  card = new Detain;
  gallery[card->id] = card;
  card = new AvarosanMarksman;
  gallery[card->id] = card;
  card = new NimblePoro;
  gallery[card->id] = card;
  card = new ScarthaneSteffen;
  gallery[card->id] = card;
  card = new NavoriConspirator;
  gallery[card->id] = card;
  card = new JubilantPoro1;
  gallery[card->id] = card;
  card = new Shatter;
  gallery[card->id] = card;
  card = new CrystalArrow1;
  gallery[card->id] = card;
  card = new ArachnoidSentry;
  gallery[card->id] = card;
  card = new RisingSpellForce;
  gallery[card->id] = card;
  card = new Mk3_Floor_b_gone1;
  gallery[card->id] = card;
  card = new JeweledProtector;
  gallery[card->id] = card;
  card = new CursedKeeper;
  gallery[card->id] = card;
  card = new Undermine1;
  gallery[card->id] = card;
  card = new Impersonate1;
  gallery[card->id] = card;
  card = new Brothers_Bond;
  gallery[card->id] = card;
  card = new HouseSpider;
  gallery[card->id] = card;
  card = new Judgment;
  gallery[card->id] = card;
  card = new UsedCaskSalesman;
  gallery[card->id] = card;
  card = new SumpDredger;
  gallery[card->id] = card;
  card = new Mk2_EvolutionTurret1;
  gallery[card->id] = card;
  card = new ScribeOfSorrows;
  gallery[card->id] = card;
  card = new Teemo_sMushroomCloud1;
  gallery[card->id] = card;
  card = new Kalista1;
  gallery[card->id] = card;
  card = new Entreat;
  gallery[card->id] = card;
  card = new NightHarvest1;
  gallery[card->id] = card;
  card = new Kalista;
  gallery[card->id] = card;
  card = new SownSeeds;
  gallery[card->id] = card;
  card = new Ezreal1;
  gallery[card->id] = card;
  card = new UnscarredReaver;
  gallery[card->id] = card;
  card = new DeathMark;
  gallery[card->id] = card;
  card = new JaeMedarda;
  gallery[card->id] = card;
  card = new ShadyCharacter;
  gallery[card->id] = card;
  card = new ShadowFlare;
  gallery[card->id] = card;
  card = new CorinaVeraza;
  gallery[card->id] = card;
  card = new CatalystOfAeons;
  gallery[card->id] = card;
  card = new Draven_sWhirlingDeath1;
  gallery[card->id] = card;
  card = new CrimsonDisciple;
  gallery[card->id] = card;
  card = new EnragedYeti1;
  gallery[card->id] = card;
  card = new MushroomCloud;
  gallery[card->id] = card;
  card = new ArenaBattlecaster;
  gallery[card->id] = card;
  card = new ZauniteUrchin;
  gallery[card->id] = card;
  card = new InspiringMentor;
  gallery[card->id] = card;
  card = new RecklessResearch1;
  gallery[card->id] = card;
  card = new BackAlleyBarkeep;
  gallery[card->id] = card;
  card = new Spiderling1;
  gallery[card->id] = card;
  card = new Scuttlegeist;
  gallery[card->id] = card;
  card = new Ashe;
  gallery[card->id] = card;
  card = new LaurentDuelist;
  gallery[card->id] = card;
  card = new TrueshotBarrage;
  gallery[card->id] = card;
  card = new MageseekerInciter;
  gallery[card->id] = card;
  card = new DawnAndDusk;
  gallery[card->id] = card;
  card = new SpiderQueenElise1;
  gallery[card->id] = card;
  card = new VanguardBannerman;
  gallery[card->id] = card;
  card = new DaringPoro;
  gallery[card->id] = card;
  card = new LegionMarauder;
  gallery[card->id] = card;
  card = new Avalanche;
  gallery[card->id] = card;
  card = new CounterfeitCopies;
  gallery[card->id] = card;
  card = new ScaledSnapper;
  gallery[card->id] = card;
  card = new YetiYearling;
  gallery[card->id] = card;
  card = new SinisterPoro;
  gallery[card->id] = card;
  card = new CrowdFavorite;
  gallery[card->id] = card;
  card = new ShadowAssassin;
  gallery[card->id] = card;
  card = new ScaledSnapper1;
  gallery[card->id] = card;
  card = new BoomcrewRookie;
  gallery[card->id] = card;
  card = new Draven;
  gallery[card->id] = card;
  card = new MysticShot;
  gallery[card->id] = card;
  card = new Anivia_sHarshWinds1;
  gallery[card->id] = card;
  card = new WarChefs;
  gallery[card->id] = card;
  card = new Ezreal;
  gallery[card->id] = card;
  card = new ScaledSnapper2;
  gallery[card->id] = card;
  card = new EscapedAbomination1;
  gallery[card->id] = card;
  card = new T_hex1;
  gallery[card->id] = card;
  card = new Reckoning;
  gallery[card->id] = card;
  card = new HextechTransmogulator;
  gallery[card->id] = card;
  card = new Darius_sDecimate1;
  gallery[card->id] = card;
  card = new Thresh;
  gallery[card->id] = card;
  card = new Lucian1;
  gallery[card->id] = card;
  card = new PluckyPoro;
  gallery[card->id] = card;
  card = new Garen_sJudgment1;
  gallery[card->id] = card;
  card = new CommanderLedros;
  gallery[card->id] = card;
  card = new MidenstokkeHenchmen;
  gallery[card->id] = card;
  card = new TheHarrowing;
  gallery[card->id] = card;
  card = new TheUndying;
  gallery[card->id] = card;
  card = new Sabotage1;
  gallery[card->id] = card;
  card = new StalkingWolf;
  gallery[card->id] = card;
  card = new Shen1;
  gallery[card->id] = card;
  card = new LaurentChevalier;
  gallery[card->id] = card;
  card = new AbsorbSoul;
  gallery[card->id] = card;
  card = new AvarosanOutriders;
  gallery[card->id] = card;
  card = new CrawlingSensation;
  gallery[card->id] = card;
  card = new Braum1;
  gallery[card->id] = card;
  card = new ShadowFiend;
  gallery[card->id] = card;
  card = new StatikkShock;
  gallery[card->id] = card;
  card = new SpectralRider1;
  gallery[card->id] = card;
  card = new TorturedProdigy;
  gallery[card->id] = card;
  card = new AlphaWildclaw;
  gallery[card->id] = card;
  card = new IntimidatingRoar;
  gallery[card->id] = card;
  card = new CrimsonPact1;
  gallery[card->id] = card;
  card = new GoldenCrushbot;
  gallery[card->id] = card;
  card = new LegionVeteran;
  gallery[card->id] = card;
  card = new BrittleSteel;
  gallery[card->id] = card;
  card = new ShirazaTheBlade;
  gallery[card->id] = card;
  card = new ObliviousIslander;
  gallery[card->id] = card;
  card = new Thresh_sTheBox1;
  gallery[card->id] = card;
  card = new Rummage;
  gallery[card->id] = card;
  card = new Katarina1;
  gallery[card->id] = card;
  card = new ScarmotherVrynna;
  gallery[card->id] = card;
  card = new ScrapdashAssembly;
  gallery[card->id] = card;
  card = new IronHarbinger;
  gallery[card->id] = card;
  card = new BlackSpear;
  gallery[card->id] = card;
  card = new HaplessAristocrat;
  gallery[card->id] = card;
  card = new Rush;
  gallery[card->id] = card;
  card = new Heimerdinger_sProgressDay_1;
  gallery[card->id] = card;
  card = new Winter_sBreath;
  gallery[card->id] = card;
  card = new Remembrance;
  gallery[card->id] = card;
  card = new StirredSpirits;
  gallery[card->id] = card;
  card = new ThermogenicBeam;
  gallery[card->id] = card;
  card = new Fiora1;
  gallery[card->id] = card;
  card = new AvarosanHearthguard;
  gallery[card->id] = card;
  card = new CaptainFarron;
  gallery[card->id] = card;
  card = new PoroHerder;
  gallery[card->id] = card;
  card = new Lux1;
  gallery[card->id] = card;
  card = new LegionRearguard;
  gallery[card->id] = card;
  card = new MageseekerConservator;
  gallery[card->id] = card;
  card = new UnleashedSpirit1;
  gallery[card->id] = card;
  card = new VanguardFirstblade;
  gallery[card->id] = card;
  card = new Blade_sEdge;
  gallery[card->id] = card;
  card = new SilverwingScout;
  gallery[card->id] = card;
  card = new StaggeringStrikes1;
  gallery[card->id] = card;
  card = new Kalista_sBlackSpear1;
  gallery[card->id] = card;
  card = new LegionGrenadier;
  gallery[card->id] = card;
  card = new Decimate;
  gallery[card->id] = card;
  card = new ZephyrSage;
  gallery[card->id] = card;
  card = new FrenziedSkitterer;
  gallery[card->id] = card;
  card = new LegionGeneral;
  gallery[card->id] = card;
  card = new LegionDrummer;
  gallery[card->id] = card;
  card = new SilverwingVanguard;
  gallery[card->id] = card;
  card = new Vengeance;
  gallery[card->id] = card;
  card = new LaurentProtege;
  gallery[card->id] = card;
  card = new GreengladeLookout;
  gallery[card->id] = card;
  card = new AstuteAcademic;
  gallery[card->id] = card;
  card = new Teemo;
  gallery[card->id] = card;
  card = new Mk5_RocketBlaster1;
  gallery[card->id] = card;
  card = new UnlicensedInnovation;
  gallery[card->id] = card;
  card = new Succession;
  gallery[card->id] = card;
  card = new ArenaBookie;
  gallery[card->id] = card;
  card = new WhirlingDeath;
  gallery[card->id] = card;
  card = new VanguardLookout;
  gallery[card->id] = card;
  card = new ScarmaidenReaver;
  gallery[card->id] = card;
  card = new Purify;
  gallery[card->id] = card;
  card = new Tarkaz_sFury1;
  gallery[card->id] = card;
  card = new HeartOfTheFluft;
  gallery[card->id] = card;
  card = new SnowHare1;
  gallery[card->id] = card;
  card = new WyrdingStones;
  gallery[card->id] = card;
  card = new TheBox;
  gallery[card->id] = card;
  card = new FinalSpark1;
  gallery[card->id] = card;
  card = new MinahSwiftfoot;
  gallery[card->id] = card;
  card = new Vision;
  gallery[card->id] = card;
  card = new IcevaleArcher;
  gallery[card->id] = card;
  card = new SumpsnipeScavenger;
  gallery[card->id] = card;
  card = new ElixirOfIron;
  gallery[card->id] = card;
  card = new KindlyTavernkeeper;
  gallery[card->id] = card;
  card = new IllegalContraption1;
  gallery[card->id] = card;
  card = new TrifarianShieldbreaker;
  gallery[card->id] = card;
  card = new Tryndamere1;
  gallery[card->id] = card;
  card = new Ashe1;
  gallery[card->id] = card;
  card = new AcceleratedPurrsuit1;
  gallery[card->id] = card;
  card = new Yasuo;
  gallery[card->id] = card;
  card = new RitualOfRenewal;
  gallery[card->id] = card;
  card = new DauntlessVanguard1;
  gallery[card->id] = card;
  card = new StandUnited;
  gallery[card->id] = card;
  card = new TwinDisciplines;
  gallery[card->id] = card;
  card = new Balesight1;
  gallery[card->id] = card;
  card = new MageseekerPersuader;
  gallery[card->id] = card;
  card = new MinotaurReckoner;
  gallery[card->id] = card;
  card = new BatteringRam;
  gallery[card->id] = card;
  card = new VanguardSquire;
  gallery[card->id] = card;
  card = new RavenousButcher;
  gallery[card->id] = card;
  card = new Zed_sShadowshift1;
  gallery[card->id] = card;
  card = new Hecarim_sOnslaughtOfShadows1;
  gallery[card->id] = card;
  card = new Riposte;
  gallery[card->id] = card;
  card = new Mk7_ArmoredStomper1;
  gallery[card->id] = card;
  card = new FreshOfferings;
  gallery[card->id] = card;
  card = new AssemblyBot;
  gallery[card->id] = card;
  card = new WindfarerHatchling;
  gallery[card->id] = card;
  card = new PoroSnax;
  gallery[card->id] = card;
  card = new CrimsonAristocrat;
  gallery[card->id] = card;
  card = new Jinx1;
  gallery[card->id] = card;
  card = new BattleFury;
  gallery[card->id] = card;
  card = new Reinforcements;
  gallery[card->id] = card;
  card = new ChainVest;
  gallery[card->id] = card;
  card = new Zed;
  gallery[card->id] = card;
  card = new Katarina_sDeathLotus1;
  gallery[card->id] = card;
  card = new TrifarianHopeful;
  gallery[card->id] = card;
  card = new Braum_sTakeHeart1;
  gallery[card->id] = card;
  card = new Mistwraith;
  gallery[card->id] = card;
  card = new ElixirOfWrath;
  gallery[card->id] = card;
  card = new SilentShadowseer;
  gallery[card->id] = card;
  card = new HauntedRelic;
  gallery[card->id] = card;
  card = new MarkOfTheIsles;
  gallery[card->id] = card;
  card = new Shen;
  gallery[card->id] = card;
  card = new PreciousPet;
  gallery[card->id] = card;
  card = new GraspOfTheUndying;
  gallery[card->id] = card;
  card = new ChempunkShredder;
  gallery[card->id] = card;
  card = new LonelyPoro;
  gallery[card->id] = card;
  card = new Karma;
  gallery[card->id] = card;
  card = new Possession;
  gallery[card->id] = card;
  card = new RadiantGuardian;
  gallery[card->id] = card;
  card = new EminentBenefactor;
  gallery[card->id] = card;
  card = new DeathLotus;
  gallery[card->id] = card;
  card = new Mk0_WindupShredder1;
  gallery[card->id] = card;
  card = new CloudDrinker;
  gallery[card->id] = card;
  card = new LaurentBladekeeper;
  gallery[card->id] = card;
  card = new Warmother_sCall;
  gallery[card->id] = card;
  card = new PurrsuitOfPerfection;
  gallery[card->id] = card;
  card = new BladeOfLedros1;
  gallery[card->id] = card;
  card = new Funsmith;
  gallery[card->id] = card;
  card = new SplinterSoul;
  gallery[card->id] = card;
  card = new Atrocity;
  gallery[card->id] = card;
  card = new Yasuo_sSteelTempest1;
  gallery[card->id] = card;
  card = new Recall;
  gallery[card->id] = card;
  card = new LivingShadow1;
  gallery[card->id] = card;
  card = new WillOfIonia;
  gallery[card->id] = card;
  card = new FatalStrike1;
  gallery[card->id] = card;
  card = new FeralMystic;
  gallery[card->id] = card;
  card = new SkywardStrikes1;
  gallery[card->id] = card;
  card = new NavoriBladescout;
  gallery[card->id] = card;
  card = new IcyYeti;
  gallery[card->id] = card;
  card = new Shunpo;
  gallery[card->id] = card;
  card = new PoisonPuffcap1;
  gallery[card->id] = card;
  card = new Face_melter1;
  gallery[card->id] = card;
  card = new Mist_sCall;
  gallery[card->id] = card;
  card = new Catastrophe1;
  gallery[card->id] = card;
  card = new TrifarianAssessor;
  gallery[card->id] = card;
  card = new PrismaticBarrier;
  gallery[card->id] = card;
  card = new Vladimir1;
  gallery[card->id] = card;
  card = new ProfessorVonYipp;
  gallery[card->id] = card;
  card = new Mk1_Wrenchbot1;
  gallery[card->id] = card;
  card = new Tryndamere;
  gallery[card->id] = card;
  card = new BullElnuk;
  gallery[card->id] = card;
  card = new BroodAwakening;
  gallery[card->id] = card;
  card = new PackMentality;
  gallery[card->id] = card;
  card = new UnstableVoltician;
  gallery[card->id] = card;
  card = new Wraithcaller;
  gallery[card->id] = card;
  card = new SparringStudent;
  gallery[card->id] = card;
  card = new Ashe_sFlashFreeze1;
  gallery[card->id] = card;
  card = new TarkazTheTribeless;
  gallery[card->id] = card;
  card = new GreengladeDuo;
  gallery[card->id] = card;
  card = new BloodForBlood;
  gallery[card->id] = card;
  card = new KinkouWayfinder;
  gallery[card->id] = card;
  card = new SingleCombat;
  gallery[card->id] = card;
  card = new Katarina;
  gallery[card->id] = card;
  card = new SharkChariot;
  gallery[card->id] = card;
  card = new AvarosanTrapper;
  gallery[card->id] = card;
  card = new ProgressDay_;
  gallery[card->id] = card;
  card = new Yusari;
  gallery[card->id] = card;
  card = new TroopOfElnuks;
  gallery[card->id] = card;
  card = new HealthPotion;
  gallery[card->id] = card;
  card = new ChroniclerOfRuin;
  gallery[card->id] = card;
  card = new PlazaGuardian;
  gallery[card->id] = card;
  card = new RenShadowblade;
  gallery[card->id] = card;
  card = new Anivia1;
  gallery[card->id] = card;
  card = new EtherealRemitter;
  gallery[card->id] = card;
  card = new ClumpOfWhumps;
  gallery[card->id] = card;
  card = new EmeraldAwakener;
  gallery[card->id] = card;
  card = new TakeHeart;
  gallery[card->id] = card;
  card = new SavageReckoner;
  gallery[card->id] = card;
  card = new EnGarde;
  gallery[card->id] = card;
  card = new SpectralMatron;
  gallery[card->id] = card;
  card = new RimetuskShaman;
  gallery[card->id] = card;
  card = new Deny;
  gallery[card->id] = card;
  card = new TrifarianGloryseeker;
  gallery[card->id] = card;
  card = new FlameChompers_;
  gallery[card->id] = card;
  card = new AncientYeti;
  gallery[card->id] = card;
  card = new VanguardSergeant;
  gallery[card->id] = card;
  card = new Bullseye1;
  gallery[card->id] = card;
  card = new MightyPoro;
  gallery[card->id] = card;
  card = new NavoriBrigand1;
  gallery[card->id] = card;
  card = new ArachnoidHorror;
  gallery[card->id] = card;
  card = new GetExcited_;
  gallery[card->id] = card;
  card = new Mk6_Stormlobber1;
  gallery[card->id] = card;
  card = new Senna_SentinelOfLight;
  gallery[card->id] = card;
  card = new BloodswornPledge;
  gallery[card->id] = card;
  card = new NavoriHighwayman;
  gallery[card->id] = card;
  card = new TheEmpyrean;
  gallery[card->id] = card;
  card = new BackToBack;
  gallery[card->id] = card;
  card = new Vladimir;
  gallery[card->id] = card;
  card = new FlashOfBrilliance;
  gallery[card->id] = card;
  card = new Ezreal_sMysticShot1;
  gallery[card->id] = card;
  card = new ForDemacia_;
  gallery[card->id] = card;
  card = new BasiliskRider;
  gallery[card->id] = card;
  card = new GreengladeCaretaker;
  gallery[card->id] = card;
  card = new RadiantStrike;
  gallery[card->id] = card;
  card = new InsightOfAges;
  gallery[card->id] = card;
  card = new TheRekindler;
  gallery[card->id] = card;
  card = new FlashFreeze;
  gallery[card->id] = card;
  card = new ChumpWhump;
  gallery[card->id] = card;
  card = new Hecarim;
  gallery[card->id] = card;
  card = new BrightsteelProtector;
  gallery[card->id] = card;
  card = new WitheringWail;
  gallery[card->id] = card;
  card = new AmateurAeronaut;
  gallery[card->id] = card;
  card = new Shen_sStandUnited1;
  gallery[card->id] = card;
  card = new RhasaTheSunderer;
  gallery[card->id] = card;
  card = new Anivia;
  gallery[card->id] = card;
  card = new AffectionatePoro;
  gallery[card->id] = card;
  card = new IntrepidMariner;
  gallery[card->id] = card;
  card = new Lux;
  gallery[card->id] = card;
  card = new Fiora_sRiposte1;
  gallery[card->id] = card;
  card = new Draven_sBiggestFan;
  gallery[card->id] = card;
  card = new GlacialStorm2;
  gallery[card->id] = card;
  card = new RecklessTrifarian;
  gallery[card->id] = card;
  card = new DarkwaterScourge;
  gallery[card->id] = card;
  card = new SteelTempest;
  gallery[card->id] = card;
  card = new CithriaOfCloudfield;
  gallery[card->id] = card;
  card = new Vladimir_sTransfusion1;
  gallery[card->id] = card;
  card = new FluftOfPoros1;
  gallery[card->id] = card;
  card = new FleetfeatherTracker;
  gallery[card->id] = card;
  card = new Spirit_sRefuge;
  gallery[card->id] = card;
  card = new Mk4_ApexTurret1;
  gallery[card->id] = card;
  card = new Eggnivia1;
  gallery[card->id] = card;
  card = new SumpworksMap;
  gallery[card->id] = card;
  card = new Might;
  gallery[card->id] = card;
  card = new VanguardCavalry;
  gallery[card->id] = card;
  card = new Rivershaper;
  gallery[card->id] = card;
  card = new ChempunkPickpocket;
  gallery[card->id] = card;
  card = new StarlitSeer;
  gallery[card->id] = card;
  card = new SolitaryMonk;
  gallery[card->id] = card;
  card = new SheWhoWanders;
  gallery[card->id] = card;
  card = new TallTales;
  gallery[card->id] = card;
  card = new NoxianGuillotine;
  gallery[card->id] = card;
  card = new CausticCask;
  gallery[card->id] = card;
  card = new Warden_sPrey;
  gallery[card->id] = card;
  card = new DisciplineOfForce1;
  gallery[card->id] = card;
  card = new VanguardRedeemer;
  gallery[card->id] = card;
  card = new Karma_sInsightOfAges1;
  gallery[card->id] = card;
  card = new Shadowshift;
  gallery[card->id] = card;
  card = new CrimsonAwakener;
  gallery[card->id] = card;
  card = new MageseekerInvestigator;
  gallery[card->id] = card;
  card = new KatoTheArm;
  gallery[card->id] = card;
  card = new VileFeast;
  gallery[card->id] = card;
  card = new TheyWhoEndure;
  gallery[card->id] = card;
  card = new CithriaTheBold;
  gallery[card->id] = card;
  card = new Yone_Windchaser;
  gallery[card->id] = card;
  card = new KiGuardian;
  gallery[card->id] = card;
  card = new RedoubledValor;
  gallery[card->id] = card;
  card = new TheRuination;
  gallery[card->id] = card;
  card = new Thresh1;
  gallery[card->id] = card;
  card = new Guile;
  gallery[card->id] = card;
  card = new BabblingBjerg;
  gallery[card->id] = card;
  card = new Garen;
  gallery[card->id] = card;
  card = new SpinningAxe1;
  gallery[card->id] = card;
  card = new AvarosanSentry;
  gallery[card->id] = card;
  card = new Soulgorger;
  gallery[card->id] = card;
  card = new FadingMemories;
  gallery[card->id] = card;
}

void init_gallery() {
  init_gallery_();
  for(auto p: gallery){
    if(p.second->collectible)
      collectible[p.first] = p.second;
  }
}

void clear_gallery() {
  for(auto entry: gallery){
    delete entry.second;
  }
  gallery.clear();
}
